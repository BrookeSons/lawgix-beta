<table>
  <thead>
  <tr>
    <td>Requests</td>
    <td>Status</td>
  </tr>
  </thead>
  <tbody>

  <% @services = @project.services %>
  <% @services.each do |service| %>
      <tr>
        <td><%= link_to service.request, service %></td>
        <td><%= service.current_state %></td>
        <% end %>
     </tr>
  </tbody>
</table>
