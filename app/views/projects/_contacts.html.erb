<table>
  <thead>
  <tr>
    <td>Contacts</td>
    <td>Email</td>
  </tr>
  </thead>
  <tbody>

  <% @contacts = @project.contacts %>
  <% @contacts.each do |contact| %>
      <tr>
        <td><%= link_to contact.last_name, contact %></td>
        <td><%= contact.email %></td>
  <% end %>
  </tr>
  </tbody>
</table>
