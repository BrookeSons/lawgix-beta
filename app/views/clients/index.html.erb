<div id="client-index">
  <h1>Clients</h1>
  <table>
    <thead>
    <tr>
      <td>Name</td>
      <td>Billing Contact</td>
      <td>Phone</td>
      <td>Email<td>
    </tr>
    </thead>
    <tbody>
    <% @clients.each do |client| %>
        <tr>
          <td><%= link_to(client.last_name, client_path(client))  %></td>
          <td><%= client.phone %></td>
          <td><%= client.email %><td>
        </tr>
    <% end %>
    </tbody>
  </table>
</div>
